{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug with LLDB",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/build/obstacle_maker_cpp",
            "args": [],
            "cwd": "${workspaceFolder}/build",
            "stopOnEntry": false,
            "terminal": "integrated"
        }
    ]
}
